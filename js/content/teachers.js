const renderTeachers = () => {
    const contentContainer = document.getElementById('content-container');
    contentContainer.innerHTML += `<div>TEACHERS</div>
  `;
  };
  
  export {renderTeachers};